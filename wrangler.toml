# Pages Git builds read this file. Do not put a D1 database_id here until
# those databases exist. Bind `DB` in the Pages dashboard per environment.
name = "thearchiveof"
compatibility_date = "2026-09-01"
pages_build_output_dir = "dist"
